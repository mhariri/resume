Running Locally
==============
Run in project directory:

```
python3 -m http.server
```
And browse to: [http://localhost:8000](http://localhost:8000)

Convert to PDF
==============
Run:
```
./build.sh
```